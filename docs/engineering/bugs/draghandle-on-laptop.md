Creator  mode long press doesn't work on Drag handles on laptop -  we had same issue with clickability..but cause may not be same

codex - I do not recommend doing this as a follow-on pointer-capture fix. Technical reason: the drag handles already receive dnd-kit listeners at [CreatorDragLab.tsx (line 665)](/Users/peterdomokos/projects/switchplay-app/src/components/creator/CreatorDragLab.tsx:665) and [CreatorDragLab.tsx (line 738)](/Users/peterdomokos/projects/switchplay-app/src/components/creator/CreatorDragLab.tsx:738), but the Creator only configures TouchSensor at [CreatorDragLab.tsx (line 873)](/Users/peterdomokos/projects/switchplay-app/src/components/creator/CreatorDragLab.tsx:873). In dnd-kit, TouchSensor activates from onTouchStart, not desktop mouse events. So desktop mouse long-press is not failing because pointer capture retargets it; it has no mouse drag sensor path configured.
The recent click fix already removed immediate mouse pointer capture from the pan layer. If desktop handle long-press still does not work, that points away from the same root cause.
Expected change size: adding a desktop sensor might be small syntactically, but it is not the same root cause and would introduce interaction questions with board pan and handle drag activation. That is beyond the “same issue, equally tiny, zero-risk mobile-isolated” bar.
